# Optional. Falls back to the public devnet endpoint (clusterApiUrl("devnet")),
# which is rate limited hard enough to break under normal development.
# Point this at a dedicated devnet RPC (Helius, QuickNode, Triton, …).
NEXT_PUBLIC_SOLANA_RPC_ENDPOINT=
